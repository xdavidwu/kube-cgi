name: test
on: workflow_call
jobs:
  test:
    runs-on: ubuntu-latest
    container: alpine:edge
    steps:
      - uses: actions/checkout@v3
      - name: prepare
        run: apk add make bash go
      - name: test
        run: make test
